/**
 * Automatic `$pageview` events: `usePosthogPageTracking` in `PosthogAnalytics`.
 * For enriched page context, import `capturePageView` from `@/lib/analytics`.
 */
export { capturePageView } from '@/lib/analytics';
