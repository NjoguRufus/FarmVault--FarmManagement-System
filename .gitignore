# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (sensitive: keys, URLs, secrets)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.private
*.env

# Supabase local metadata (no secrets in schema files; temp state only)
supabase/.temp/
supabase/.branches/
supabase/.remote/

# Phase 0 local backup output (never commit)
backups/

# Local databases / dumps
*.sqlite
*.sqlite3
*.db
*.db-journal
*.sql.gz
*.dump

# Sensitive: keys, credentials, service accounts
*.pem
*.key
!*.key.pub
secrets/
.secrets/
*credentials*.json
*secret*.json
service-account*.json
firebase-adminsdk*.json
*-credentials.json

# Audit (local only)
docs/COMPREHENSIVE_SYSTEM_AUDIT.md
